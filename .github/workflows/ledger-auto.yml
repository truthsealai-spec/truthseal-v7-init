name: Ledger — OTS Stamp & Verify (manual)

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto-stamping temporarily paused to clear red noise; run manually when ready."
