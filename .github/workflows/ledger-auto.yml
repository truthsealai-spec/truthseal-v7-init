name: Ledger — OTS Stamp & Verify (manual)
on:
  workflow_dispatch: {}

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily paused to stop CI noise while we finish anchoring & secrets."
