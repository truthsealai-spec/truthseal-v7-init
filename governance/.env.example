# TruthSeal – docker runtime defaults (sample; copy to .env and change)
DOCKER_VOLUME_DIRECTORY=.

# pgvector / Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGEME_strong_password
POSTGRES_DB=api

# GPU selection for Milvus (0 = first GPU)
VECTORSTORE_GPU_DEVICE_ID=0

# MinIO (matches compose)
MINIO_ACCESS_KEY=CHANGEME_minio_access
MINIO_SECRET_KEY=CHANGEME_minio_secret
