# TruthSeal — docker runtime defaults (sample; copy to .env and edit)
# Volumes base (relative to repo)
DOCKER_VOLUME_DIRECTORY=.

# pgvector / Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=api

# GPU selection for Milvus (0 = first GPU)
VECTORSTORE_GPU_DEVICE_ID=0

# MinIO (matches compose)
MINIO_ACCESS_KEY=CHANGE_ME
MINIO_SECRET_KEY=CHANGE_ME
